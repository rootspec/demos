---
---
<section data-test="hero" style="padding:5rem 1.5rem 4rem;max-width:860px;margin:0 auto;">
  <h1 style="font-size:clamp(2rem,5vw,3.2rem);font-weight:300;line-height:1.15;letter-spacing:-0.02em;color:var(--color-text);margin-bottom:1.5rem;max-width:640px;">
    Specification that stays<br/>honest under pressure.
  </h1>
  <p style="font-size:1.15rem;color:var(--color-text-muted);max-width:560px;line-height:1.7;margin-bottom:2rem;">
    RootSpec is a declarative methodology where philosophy drives implementation — never the reverse. Every user story traces back to a design pillar. Every test validates the original intent.
  </p>
  <p style="font-size:0.9rem;font-family:'Inter',sans-serif;color:var(--color-text-muted);border-left:2px solid var(--color-border);padding-left:1rem;font-style:italic;">
    "When code and specs can be generated trivially, the real value is validation and proof."
  </p>
</section>
